## ALL()
MEASURE2 = 
CALCULATE(
    [MEASURE1],
    ALL(
        'Table'
    )
)

## REMOVEFILTERS()
## Can only be used in CALCULATE(), not a table function.
MEASURE2 = 
CALCULATE(
    [MEASURE1],
    REMOVEFILTERS(
        'Table'
    )
)
